$kind: http-request
name: Get Coaches by Status
method: GET
url: '{{baseUrl}}/api/v1/coaches/status/{{status}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", () => pm.response.to.have.status(200));
      pm.test("Response is an array", () => {
        const json = pm.response.json();
        pm.expect(json).to.be.an("array");
      });
