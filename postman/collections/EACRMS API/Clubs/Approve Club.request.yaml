$kind: http-request
name: Approve Club
method: PATCH
url: '{{baseUrl}}/api/v1/clubs/{{clubId}}/approve'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", () => pm.response.to.have.status(200));
